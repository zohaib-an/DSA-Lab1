The function 'function3.cpp' searches an array for a key and returns a vector that includes the indices for that key in that arary.
The file 'main.cpp' has three different cases where this function is tested and verified.
